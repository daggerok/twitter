plugins {
    id 'org.springframework.boot' version '1.5.2.RELEASE'
}

jar {
    baseName = 'eureka-config-server'
}

dependencies {
    compile 'org.springframework.cloud:spring-cloud-config-server'
}

springBoot {
    executable = true
}
