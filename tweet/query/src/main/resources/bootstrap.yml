spring:
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:8888}
  application:
    name: tweet-query
  profiles:
    include:
      - web
      - admin
      - eureka
      - rabbitmq
      - spring-cloud-stream
server:
  port: 8080
